void main() {
            int x = 1;
            {
                int y = 2; 
                printi(x); // Access outer variable (Allowed)
                printi(y); // Access inner variable
            }
            printi(x); // Outer variable remains
        }